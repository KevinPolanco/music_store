
<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4 fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><img style="width: 60px" src="/img/JazzStore3.png" alt=""></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/store">Tienda</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/myCollection">Mis discos</a>
        </li>
          </ul>
        </li>
      </ul>
      <div>
      <div id="btnBar"></div>
    </div>
  </div>
</nav>

<script>
  const checkToken = async() => {
     try {
      
      if(localStorage.getItem('token')){
        btnBar.innerHTML = `
          <button id="logOut" class="btn btn-outline-danger btn-sm me-2">Cerrar sesión</button>
        `;
        const logOut = document.getElementById('logOut')
        logOut.addEventListener('click', async (e) => {
          e.preventDefault();
          localStorage.clear();
          window.location.href= "/login"
        })
      } else {
         btnBar.innerHTML = `
          <a href="/login"><button class="btn btn-outline-warning btn-sm me-2">Login</button></a>
          <a href="/register"><button class="btn btn-success me-2">Registrate</button></a>
        `
      }
      
    } catch (error) {
      console.log(error);
    }
  }

  
  
 checkToken();
</script>